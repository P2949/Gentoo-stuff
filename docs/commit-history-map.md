# Additive commit history map

This map gives precise descriptions to evidence-bearing ancestors whose
original commit subjects were too generic. It is explanatory metadata only:
the commits, trees, and retained evidence remain the authority for what each
revision contained.

| Commit | Additive description |
| --- | --- |
| `d8a90a41f78c20e18a83bab3f4f1a7dd418856cc` | Introduced the root-supervised production sample-profile transaction and authorization-token scanner, connected them to Portage dispatch and installer behavior, and added the corresponding crash, recovery, environment, and integration fixtures. |
| `19a46b78acafcc96df6a4f4c54b0880109734354` | Moved the production transaction helpers out of the test tree and established the Candidate A/Candidate B detached-evidence architecture: the operator runbook, evidence policy and tool manifest, component/index schemas, verifier, and repository-only end-to-end sample-PGO integration/evidence fixtures. |
| `03e8917dc69a1cda1e9d7063e16b6a2ce0ff61fa` | Introduced the deliberately live-disabled `jsonschema` prerequisite transaction: its durable state-machine and recovery groundwork, immutable Git/rsync/local repository materialization and provenance, exact resolver-plan validation, private package roots, namespace/PTY containment and authenticated child control, plus its semantic fixture suite. It also added the portable atomic-exchange adapter, strengthened framework-installer portability, and isolated the exact checkpoint-smoke methods. The revision is a rejected predecessor, not Candidate-A or Phase 2 authorization evidence. |
| `dcc6b8fc76e1bff20a25a06cda4aadda580b3885` | Extended the live-disabled `jsonschema` prerequisite far beyond the narrow unit-test subject: immutable bootstrap publication, locked and externalized Portage/VDB authority, process/handle exclusion, private build roots, payload admission and post-emerge verification, counter and recovery-failed protocols, terminal durability, expanded tests, runbook/workflow changes, and Phase-2 evidence/tool-policy integration. Exact workflow run `31880559624` rejected the revision at the pre-run contract check; it is non-authorizing predecessor evidence, not Candidate A. |
| `9dd5960958c397216dd4c60b6212658f10015dca` | Closed the bounded August-22 correction set rather than only disabling legacy BOLT: fixed prerequisite fixtures and signal transitions, strengthened exact Portage locks/counter/payload/durability and root-host capability proofs, corrected bootstrap fixture authority, retired all four legacy BOLT entrypoints, added the eight-label Candidate-B checkpoint/prerequisite chain and its first semantic verifier, and updated plan/history/driver policy. Exact workflow run `32601934693` stopped at the stale static contract before `portable-complete`; separate direct review found incomplete repository/resolver evidence semantics. The revision remains rejected and non-authorizing, both prerequisite live gates remain disabled, and its portable/CI/Candidate-A boundaries were never established. |
| `ea191dce00f19db8ea0dda20ded85ab594d40db3` | Added managed prerequisite signal-transition and parent-death integration coverage and expanded detached verification of prerequisite repository/resolver authority. Exact workflow run `33104209384` stopped at the stale static contract before `portable-complete`; separate direct review found a verifier syntax error, stale Git fixture expectations, and validator/schema wiring defects. The revision is rejected, non-authorizing predecessor evidence. |
| `0f91f5ab68d7917206e79d4cb688b3a93ab5f182` | Integrated authentic prerequisite-producer authorities into the detached evidence fixture and expanded prerequisite-chain validation. Exact workflow run `33170596519` stopped at the stale static contract before `portable-complete`; separate direct review found incompatible producer/verifier JSON digest encodings, a portable fixture that invoked production root trust on `/tmp` executables, and negative tests whose invalid base could hide the intended rejection. The revision is rejected, non-authorizing predecessor evidence. |
| `6462859e207b3545d9b699c242250022ad8c9f26` | Corrected the prerequisite evidence protocol by independently matching the producer's digest serialization, separated portable executable observations from production root trust, and made chain-tamper tests start from a valid base and assert the intended rejection. Exact workflow run `33188537276` stopped at the stale static contract before `portable-complete`; the fixes passed focused direct review but no green exact-SHA portable boundary exists. The revision is rejected, non-authorizing predecessor evidence. |
| `274c50d2208dea9a86f0ff4e7293c7cee5df5603` | Brought the tracked contract into exact agreement with the reviewed 34 top-level, 59 Bash, 44 evidence, 323 main-Python, one stress, and 79 recovery identities; admitted the four exact portable-only prerequisite host skips; added narrow ShellCheck annotations to the fail-closed legacy-BOLT stubs; and corrected the private-root fixture's mode under the driver's `umask 077`. Exact workflow run `33249132657` passed the static contract and exercised `portable-complete`: 79 top-level cases passed, one failed, and 13 produced reviewed skips; required subtests recorded 517 passes, one failure, and 26 reviewed skips, plus one diagnostic skip. The sole functional failure was `framework-installer`, whose unconditional `systemd-tmpfiles --dry-run` invocation is unsupported by Ubuntu 24.04's systemd 255. The final ledger-contract failure propagated that required failure; it was not renewed topology drift. The revision is rejected, non-authorizing predecessor evidence, and no live mutation or authorization resulted. |
| `dfa61d38b4c0486112789e3fc28aa6379449b154` | Replaced the mandatory systemd-256-only tmpfiles dry-run check with exact rule-byte validation and a disposable private-root real-create proof, retained production `root:portage` semantics, added bounded tool/version/argv/status diagnostics, and made the installer fixture enforce a pre-256 interface. Exact workflow run `33267217547` passed the static contract and exercised `portable-complete`: 79 top-level cases passed, one failed, and 13 produced reviewed skips; required subtests recorded 518 passes, one failure, and 26 reviewed skips, plus one diagnostic skip. The tmpfiles compatibility boundary passed. The sole functional failure remained `framework-installer`: portable generated-policy validation unconditionally attempted the Gentoo Portage atom parser, so Ubuntu reported the valid fixture atom `=app-misc/example-1` as invalid before the intended assignment-only rejection. Separate direct review also found a non-version-strict frozen-inventory CPV predicate, drift between the bootstrap and semantic-authority `owned_directories` schemas, generated atom syntax wider than the documented canonical plain `=CPV` form, and a double-escaped jq control-byte predicate that rejected ordinary nonempty paths containing `u`. The revision is rejected, non-authorizing predecessor evidence, and no live mutation, Candidate-A acceptance, or Phase 2 authorization resulted. |
| `3d3e660ca207d357caa1de5b537c6bba6fa83fbe` | Corrected the generated-policy and frozen-inventory boundary: portable validation no longer depends on Gentoo's Portage parser, production parser failures remain distinct, generated atoms are canonical plain `=CPV`, inventory CPVs are versioned, directory records match the semantic authority, and the jq control-byte predicate is repaired. Exact workflow run `33279170822` passed the current mature portable repository boundary with 80 top-level passes, zero failures, and 13 reviewed skips; required subtests recorded 519 passes, zero failures, and 26 reviewed skips, plus one diagnostic skip, for 546 total subtests. This is green exact-SHA portable repository evidence only. No live mutation, Candidate-A host acceptance, Candidate-B detached index, or Phase 2 authorization resulted. |
| `2128d87fcb5736f75b89ea6eed60f7db9ccc7fd4` | Tightened exact Gentoo CPV semantics and propagated the correction through the independent schemas, state, profile, evidence, framework, and recovery surfaces. Exact workflow run `33312523486` exercised the functional portable boundary with 79 top-level passes, one failure, and 13 reviewed skips; required subtests recorded 518 passes, one failure, and 26 reviewed skips. The sole required failure was pinned ShellCheck 0.11.0 reporting four `SC2015` findings in `create-binpkg-checkpoint.sh`; the CPV tightening remained valid. The revision is rejected and non-authorizing. Successor `d42a0cea53c1b1dfdb86fa772def255d97dc2862` completed the required remediation and green rerun. |
| `d42a0cea53c1b1dfdb86fa772def255d97dc2862` | Retained strict Gentoo CPV semantics, replaced the four recovery `SC2015` branches with explicit control flow, and made the shared CPV vector contract execute across the independent validators without adding a top-level test identity. Exact workflow run `33327273287` passed with 80 top-level passes, zero failures, and 13 reviewed skips; 519 required-subtest passes, zero failures, and 26 reviewed skips; one diagnostic skip; 546 total subtests; and exit status zero. It is the green repository/pre-activation boundary for that tree, not accepted live Candidate A or Phase 2 authorization. A later failed, non-authorizing attempt to refresh the former boot-based recovery proof exposed unacceptable firmware-entry risk and triggered a successor policy that removes all boot-entry tests/requirements and makes kernel lifecycle work human-only. The attempt must never be repeated; no boot-entry recipe is part of current project authority. |

## History rule

Evidence-bearing ancestors are immutable. Do not amend, rebase, filter, or
force-rewrite them to improve historical wording. Corrections to descriptions,
review conclusions, or provenance are additive commits and documents that name
the exact original object ID. Any source correction creates a new candidate
identity and requires the gates prescribed by the current plan.
