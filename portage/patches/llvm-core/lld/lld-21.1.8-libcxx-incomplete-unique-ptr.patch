libc++ requires std::unique_ptr<T>'s default deleter to see a complete T at
destructor instantiation time.  lld 21 has a few ELF classes with inline
implicit destructors that own forward-declared types, which libstdc++ accepts
in this build but libc++ rejects.  Move those destructors out-of-line into
translation units that already include the complete definitions.

--- a/lld/ELF/Config.h
+++ b/lld/ELF/Config.h
@@ -180,6 +180,7 @@ struct VersionDefinition {
 class LinkerDriver {
 public:
   LinkerDriver(Ctx &ctx);
+  ~LinkerDriver();
   LinkerDriver(LinkerDriver &) = delete;
   void linkerMain(ArrayRef<const char *> args);
   void addFile(StringRef path, bool withLOption);
@@ -731,6 +732,7 @@ struct Ctx : CommonLinkerContext {
   uint32_t vernauxNum = 0;
 
   Ctx();
+  ~Ctx();
 
   llvm::raw_fd_ostream openAuxiliaryFile(llvm::StringRef, std::error_code &);
 
--- a/lld/ELF/Driver.cpp
+++ b/lld/ELF/Driver.cpp
@@ -96,6 +96,8 @@ static void setConfigs(Ctx &ctx, opt::InputArgList &args);
 
 Ctx::Ctx() : driver(*this) {}
 
+Ctx::~Ctx() = default;
+
 llvm::raw_fd_ostream Ctx::openAuxiliaryFile(llvm::StringRef filename,
                                             std::error_code &ec) {
   StringRef outputFile = arg.outputFile;
@@ -647,6 +649,8 @@ constexpr const char *saveTempsValues[] = {
 
 LinkerDriver::LinkerDriver(Ctx &ctx) : ctx(ctx) {}
 
+LinkerDriver::~LinkerDriver() = default;
+
 void LinkerDriver::linkerMain(ArrayRef<const char *> argsArr) {
   ELFOptTable parser;
 
--- a/lld/ELF/InputFiles.h
+++ b/lld/ELF/InputFiles.h
@@ -320,6 +320,7 @@ template <class ELFT> class ObjFile : public ELFFileBase {
 class BitcodeFile : public InputFile {
 public:
   BitcodeFile(Ctx &, MemoryBufferRef m, StringRef archiveName,
               uint64_t offsetInArchive, bool lazy);
+  ~BitcodeFile();
   static bool classof(const InputFile *f) { return f->kind() == BitcodeKind; }
   void parse();
   void parseLazy();
--- a/lld/ELF/InputFiles.cpp
+++ b/lld/ELF/InputFiles.cpp
@@ -1822,6 +1822,8 @@ BitcodeFile::BitcodeFile(Ctx &ctx, MemoryBufferRef mb, StringRef archiveName,
   osabi = getOsAbi(t);
 }
 
+BitcodeFile::~BitcodeFile() = default;
+
 static uint8_t mapVisibility(GlobalValue::VisibilityTypes gvVisibility) {
   switch (gvVisibility) {
   switch (t.getArch()) {
--- a/lld/MachO/InputFiles.h	2026-06-12 08:02:53.067156769 +0100
+++ b/lld/MachO/InputFiles.h	2026-06-12 08:02:53.115548995 +0100
@@ -313,6 +313,7 @@
   explicit BitcodeFile(MemoryBufferRef mb, StringRef archiveName,
                        uint64_t offsetInArchive, bool lazy = false,
                        bool forceHidden = false, bool compatArch = true);
+  ~BitcodeFile();
   static bool classof(const InputFile *f) { return f->kind() == BitcodeKind; }
   void parse();
 
--- a/lld/MachO/InputFiles.cpp	2026-06-12 08:02:53.067156769 +0100
+++ b/lld/MachO/InputFiles.cpp	2026-06-12 08:02:53.116121390 +0100
@@ -2366,6 +2366,8 @@
     parse();
 }
 
+BitcodeFile::~BitcodeFile() = default;
+
 void BitcodeFile::parse() {
   // Convert LTO Symbols to LLD Symbols in order to perform resolution. The
   // "winning" symbol will then be marked as Prevailing at LTO compilation
--- a/lld/wasm/InputFiles.h	2026-06-12 08:02:53.075156585 +0100
+++ b/lld/wasm/InputFiles.h	2026-06-12 08:02:53.116420560 +0100
@@ -164,6 +164,7 @@
 public:
   BitcodeFile(MemoryBufferRef m, StringRef archiveName,
               uint64_t offsetInArchive, bool lazy);
+  ~BitcodeFile();
   static bool classof(const InputFile *f) { return f->kind() == BitcodeKind; }
 
   void parse(StringRef symName);
--- a/lld/wasm/InputFiles.cpp	2026-06-12 08:02:53.075156585 +0100
+++ b/lld/wasm/InputFiles.cpp	2026-06-12 08:02:53.116717918 +0100
@@ -870,6 +870,8 @@
 
 bool BitcodeFile::doneLTO = false;
 
+BitcodeFile::~BitcodeFile() = default;
+
 void BitcodeFile::parseLazy() {
   for (auto [i, irSym] : llvm::enumerate(obj->symbols())) {
     if (irSym.isUndefined())
