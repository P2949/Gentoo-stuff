# libjxl's public DSOs must retain their established exported C++ ABI during
# the profile wave.  This file is deliberately applied after the general
# highway workaround so its visibility contract is the final package policy.
CFLAGS="${CFLAGS} -fvisibility=default"
CXXFLAGS="${CXXFLAGS} -fvisibility=default"
