COMMON_FLAGS="${CLEAN_FLAGS} ${LIB_FLAGS} -fwhole-program-vtables -fsplit-lto-unit ${LTO_FLAGS} ${UARG_FLAGS} ${GCCLIB_FLAGS}"
CFLAGS="${COMMON_FLAGS}"
CXXFLAGS="${COMMON_FLAGS}"
FCFLAGS="${COMMON_FLAGS}"
FFLAGS="${COMMON_FLAGS}"
LDFLAGS="${COMMON_FLAGS} ${LD_CLEAN_FLAGS} -Wl,--thinlto-jobs=11 -Wl,--fat-lto-objects"
