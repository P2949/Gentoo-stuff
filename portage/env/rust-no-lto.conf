# Conservative everything for cargo packages whose build script or linker
# interaction breaks under custom RUSTFLAGS/LTO (ripgrep, bpf-linker, ...).
RUSTFLAGS=""
CARGO_BUILD_RUSTFLAGS=""
CARGO_ENCODED_RUSTFLAGS=""
LDFLAGS="-Wl,-O1 -Wl,--as-needed"
CFLAGS="-O2 -pipe"
CXXFLAGS="-O2 -pipe"
