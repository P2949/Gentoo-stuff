# The rust ncurses crate's build script runs a generated ncurses program
# (genconstants) that needs a valid terminfo entry; headless emerges with
# TERM unset/unknown silently lose TRUE/FALSE from the generated bindings.
TERM="xterm-256color"
