# The profile wave must use the locally authenticated git-r3 source snapshot;
# do not make a mutable 9999 fetch part of the bounded training transaction.
EGIT_OVERRIDE_COMMIT_LIBJXL_LIBJXL="7741c8ce"
