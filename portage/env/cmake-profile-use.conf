# CMake's configure probes execute tiny profile-use test binaries. Discard
# incidental runtime profile output from those probes; the package consumes
# the authenticated profile and remains under the normal ABI/install-QA gates.
LLVM_PROFILE_FILE="/dev/null"
